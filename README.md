All the wallpapers are sorted in the corresponding branches of this repo.<br />
To pull the desired branch you can do the following:

```
git fetch
git checkout <branch-name>
git pull origin <branch-name>
```
